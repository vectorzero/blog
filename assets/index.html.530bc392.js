import{_ as o,r as n,o as s,c as r,a as t,d,w as l,e as a,b as e}from"./app.6c09f471.js";const c={},p=a(`<p>A <strong>complex number</strong> is a number that can be expressed in the form <code>a + b * i</code>, where <code>a</code> and <code>b</code> are real numbers, and <code>i</code> is a solution of the equation <code>x^2 = \u22121</code>. Because no <em>real number</em> satisfies this equation, <code>i</code> is called an <em>imaginary number</em>. For the complex number <code>a + b * i</code>, <code>a</code> is called the <em>real part</em>, and <code>b</code> is called the <em>imaginary part</em>.</p><p><img src="https://www.mathsisfun.com/numbers/images/complex-example.svg" alt="Complex Number"></p><p>A Complex Number is a combination of a Real Number and an Imaginary Number:</p><p><img src="https://www.mathsisfun.com/numbers/images/complex-number.svg" alt="Complex Number"></p><p>Geometrically, complex numbers extend the concept of the one-dimensional number line to the <em>two-dimensional complex plane</em> by using the horizontal axis for the real part and the vertical axis for the imaginary part. The complex number <code>a + b * i</code> can be identified with the point <code>(a, b)</code> in the complex plane.</p><p>A complex number whose real part is zero is said to be <em>purely imaginary</em>; the points for these numbers lie on the vertical axis of the complex plane. A complex number whose imaginary part is zero can be viewed as a <em>real number</em>; its point lies on the horizontal axis of the complex plane.</p><table><thead><tr><th style="text-align:left;">Complex Number</th><th style="text-align:center;">Real Part</th><th style="text-align:center;">Imaginary Part</th><th></th></tr></thead><tbody><tr><td style="text-align:left;">3 + 2i</td><td style="text-align:center;">3</td><td style="text-align:center;">2</td><td></td></tr><tr><td style="text-align:left;">5</td><td style="text-align:center;">5</td><td style="text-align:center;"><strong>0</strong></td><td>Purely Real</td></tr><tr><td style="text-align:left;">\u22126i</td><td style="text-align:center;"><strong>0</strong></td><td style="text-align:center;">-6</td><td>Purely Imaginary</td></tr></tbody></table><p>A complex number can be visually represented as a pair of numbers <code>(a, b)</code> forming a vector on a diagram called an <em>Argand diagram</em>, representing the <em>complex plane</em>. <code>Re</code> is the real axis, <code>Im</code> is the imaginary axis, and <code>i</code> satisfies <code>i^2 = \u22121</code>.</p><p><img src="https://upload.wikimedia.org/wikipedia/commons/a/af/Complex_number_illustration.svg" alt="Complex Number"></p><blockquote><p>Complex does not mean complicated. It means the two types of numbers, real and imaginary, together form a complex, just like a building complex (buildings joined together).</p></blockquote><h2 id="polar-form" tabindex="-1"><a class="header-anchor" href="#polar-form" aria-hidden="true">#</a> Polar Form</h2><p>An alternative way of defining a point <code>P</code> in the complex plane, other than using the x- and y-coordinates, is to use the distance of the point from <code>O</code>, the point whose coordinates are <code>(0, 0)</code> (the origin), together with the angle subtended between the positive real axis and the line segment <code>OP</code> in a counterclockwise direction. This idea leads to the polar form of complex numbers.</p><p><img src="https://upload.wikimedia.org/wikipedia/commons/7/7a/Complex_number_illustration_modarg.svg" alt="Polar Form"></p><p>The <em>absolute value</em> (or modulus or magnitude) of a complex number <code>z = x + yi</code> is:</p><p><img src="https://wikimedia.org/api/rest_v1/media/math/render/svg/b59629c801aa0ddcdf17ee489e028fb9f8d4ea75" alt="Radius"></p><p>The argument of <code>z</code> (in many applications referred to as the &quot;phase&quot;) is the angle of the radius <code>OP</code> with the positive real axis, and is written as <code>arg(z)</code>. As with the modulus, the argument can be found from the rectangular form <code>x+yi</code>:</p><p><img src="https://wikimedia.org/api/rest_v1/media/math/render/svg/7cbbdd9bb1dd5df86dd2b820b20f82995023e566" alt="Phase"></p><p>Together, <code>r</code> and <code>\u03C6</code> give another way of representing complex numbers, the polar form, as the combination of modulus and argument fully specify the position of a point on the plane. Recovering the original rectangular co-ordinates from the polar form is done by the formula called trigonometric form:</p><p><img src="https://wikimedia.org/api/rest_v1/media/math/render/svg/b03de1e1b7b049880b5e4870b68a57bc180ff6ce" alt="Polar Form"></p><p>Using Euler&#39;s formula this can be written as:</p><p><img src="https://wikimedia.org/api/rest_v1/media/math/render/svg/0a087c772212e7375cb321d83fc1fcc715cd0ed2" alt="Euler&#39;s Form"></p><h2 id="basic-operations" tabindex="-1"><a class="header-anchor" href="#basic-operations" aria-hidden="true">#</a> Basic Operations</h2><h3 id="adding" tabindex="-1"><a class="header-anchor" href="#adding" aria-hidden="true">#</a> Adding</h3><p>To add two complex numbers we add each part separately:</p><div class="language-text ext-text"><pre class="language-text"><code>(a + b * i) + (c + d * i) = (a + c) + (b + d) * i
</code></pre></div><p><strong>Example</strong></p><div class="language-text ext-text"><pre class="language-text"><code>(3 + 5i) + (4 \u2212 3i) = (3 + 4) + (5 \u2212 3)i = 7 + 2i
</code></pre></div><p>On complex plane the adding operation will look like the following:</p><p><img src="https://www.mathsisfun.com/algebra/images/complex-plane-vector-add.svg" alt="Complex Addition"></p><h3 id="subtracting" tabindex="-1"><a class="header-anchor" href="#subtracting" aria-hidden="true">#</a> Subtracting</h3><p>To subtract two complex numbers we subtract each part separately:</p><div class="language-text ext-text"><pre class="language-text"><code>(a + b * i) - (c + d * i) = (a - c) + (b - d) * i
</code></pre></div><p><strong>Example</strong></p><div class="language-text ext-text"><pre class="language-text"><code>(3 + 5i) - (4 \u2212 3i) = (3 - 4) + (5 + 3)i = -1 + 8i
</code></pre></div><h3 id="multiplying" tabindex="-1"><a class="header-anchor" href="#multiplying" aria-hidden="true">#</a> Multiplying</h3><p>To multiply complex numbers each part of the first complex number gets multiplied by each part of the second complex number:</p>`,36),m=e('Just use "FOIL", which stands for "'),h=t("strong",null,"F",-1),g=e("irsts, "),u=t("strong",null,"O",-1),x=e("uters, "),b=t("strong",null,"I",-1),f=e("nners, "),w=t("strong",null,"L",-1),v=e('asts" ( see '),_=e("Binomial Multiplication"),y=e(" for more details):"),k=a(`<p><img src="https://www.mathsisfun.com/algebra/images/foil-complex.svg" alt="Complex Multiplication"></p><ul><li>Firsts: <code>a \xD7 c</code></li><li>Outers: <code>a \xD7 di</code></li><li>Inners: <code>bi \xD7 c</code></li><li>Lasts: <code>bi \xD7 di</code></li></ul><p>In general it looks like this:</p><div class="language-text ext-text"><pre class="language-text"><code>(a + bi)(c + di) = ac + adi + bci + bdi^2
</code></pre></div><p>But there is also a quicker way!</p><p>Use this rule:</p><div class="language-text ext-text"><pre class="language-text"><code>(a + bi)(c + di) = (ac \u2212 bd) + (ad + bc)i
</code></pre></div><p><strong>Example</strong></p><div class="language-text ext-text"><pre class="language-text"><code>(3 + 2i)(1 + 7i)
= 3\xD71 + 3\xD77i + 2i\xD71+ 2i\xD77i
= 3 + 21i + 2i + 14i^2
= 3 + 21i + 2i \u2212 14   (because i^2 = \u22121)
= \u221211 + 23i
</code></pre></div><div class="language-text ext-text"><pre class="language-text"><code>(3 + 2i)(1 + 7i) = (3\xD71 \u2212 2\xD77) + (3\xD77 + 2\xD71)i = \u221211 + 23i
</code></pre></div><h3 id="conjugates" tabindex="-1"><a class="header-anchor" href="#conjugates" aria-hidden="true">#</a> Conjugates</h3><p>We will need to know about conjugates in a minute!</p><p>A conjugate is where we change the sign in the middle like this:</p><p><img src="https://www.mathsisfun.com/numbers/images/complex-conjugate.svg" alt="Complex Conjugate"></p><p>A conjugate is often written with a bar over it:</p><div class="language-text ext-text"><pre class="language-text"><code>______
5 \u2212 3i   =   5 + 3i
</code></pre></div><p>On the complex plane the conjugate number will be mirrored against real axes.</p><p><img src="https://upload.wikimedia.org/wikipedia/commons/6/69/Complex_conjugate_picture.svg" alt="Complex Conjugate"></p><h3 id="dividing" tabindex="-1"><a class="header-anchor" href="#dividing" aria-hidden="true">#</a> Dividing</h3><p>The conjugate is used to help complex division.</p><p>The trick is to <em>multiply both top and bottom by the conjugate of the bottom</em>.</p><p><strong>Example</strong></p><div class="language-text ext-text"><pre class="language-text"><code>2 + 3i
------
4 \u2212 5i
</code></pre></div><p>Multiply top and bottom by the conjugate of <code>4 \u2212 5i</code>:</p><div class="language-text ext-text"><pre class="language-text"><code>  (2 + 3i) * (4 + 5i)   8 + 10i + 12i + 15i^2
= ------------------- = ----------------------
  (4 \u2212 5i) * (4 + 5i)   16 + 20i \u2212 20i \u2212 25i^2
</code></pre></div><p>Now remember that <code>i^2 = \u22121</code>, so:</p><div class="language-text ext-text"><pre class="language-text"><code>  8 + 10i + 12i \u2212 15    \u22127 + 22i   \u22127   22
= ------------------- = -------- = -- + -- * i
  16 + 20i \u2212 20i + 25      41      41   41

</code></pre></div><p>There is a faster way though.</p><p>In the previous example, what happened on the bottom was interesting:</p><div class="language-text ext-text"><pre class="language-text"><code>(4 \u2212 5i)(4 + 5i) = 16 + 20i \u2212 20i \u2212 25i
</code></pre></div><p>The middle terms <code>(20i \u2212 20i)</code> cancel out! Also <code>i^2 = \u22121</code> so we end up with this:</p><div class="language-text ext-text"><pre class="language-text"><code>(4 \u2212 5i)(4 + 5i) = 4^2 + 5^2
</code></pre></div><p>Which is really quite a simple result. The general rule is:</p><div class="language-text ext-text"><pre class="language-text"><code>(a + bi)(a \u2212 bi) = a^2 + b^2
</code></pre></div><h2 id="references" tabindex="-1"><a class="header-anchor" href="#references" aria-hidden="true">#</a> References</h2><ul><li><a href="https://en.wikipedia.org/wiki/Complex_number" target="_blank" rel="noopener noreferrer">Wikipedia</a></li><li><a href="https://www.mathsisfun.com/numbers/complex-numbers.html" target="_blank" rel="noopener noreferrer">Math is Fun</a></li></ul>`,36);function C(j,T){const i=n("RouterLink");return s(),r("div",null,[p,t("p",null,[m,h,g,u,x,b,f,w,v,d(i,{to:"/posts/algorithms/math/ttps:/www.mathsisfun.com/algebra/polynomials-multiplying.html"},{default:l(()=>[_]),_:1}),y]),k])}var N=o(c,[["render",C],["__file","index.html.vue"]]);export{N as default};
